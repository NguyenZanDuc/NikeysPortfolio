/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['theplannersvn.com'],
        domains: ['cdn.dribbble.com'],
    },
}

module.exports = nextConfig
